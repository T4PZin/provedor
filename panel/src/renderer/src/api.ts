import type { KnownPlayer, PaintEntry, Player, ServerStatus, Settings } from '../../types'

export const api = window.fraghost

export type { KnownPlayer, PaintEntry, Player, ServerStatus, Settings }
